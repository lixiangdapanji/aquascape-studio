# Placeholder — infra-agent wires this up in Phase 2 once ECS Fargate service
# exists in AppStack and ECR repo accepts pushes.
name: deploy-web
on:
  workflow_dispatch:
    inputs:
      environment:
        type: choice
        options: [dev, stage, prod]
        default: dev
permissions:
  id-token: write
  contents: read
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Phase 2 work. See docs/infra-runbook.md."
